export class Knight {
	constructor(config) {
		this.id = config.id;
		this.name = config.name;
		this.emoji = config.emoji;
		this.model = config.model;
		this.color = config.color;
		glow;
		active;
		tokens;
	}
}
